<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>商品修改</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css" />
</head>
<body>
    <form role="form" action='update' method='post' enctype="multipart/form-data">
        <div><input type="hidden" name="id" value="<%= product.id %>"></div>
        <div class="form-group">
            <label for="name">商品名称：</label><input class="form-control" type="text" name="productName" value="<%= product.name %>">
        </div>
        <div class="form-group">
             <label>价格：</label><input class="form-control" type="text" name="price" value="<%= product.price %>">
        </div>
        <div class="form-group">
             <label>描述：</label><input class="form-control" type="text" name="description" value="<%= product.description %>">
        </div>
        <div class="form-group">
             <label>数量：</label><input class="form-control" type="number" name="number" value="<%= product.number %>">
        </div>
        <div class="form-group">
            <label for="name">商品类别:</label>
            <select name="category">
                <% for(var i=0;i<list.length;i++) {%>
                    <option value ="<%= list[i].t_id%>"><%= list[i].t_name%></option>
                    <% } %>
            </select>
        </div>
        <div class="form-group">
             <label>修改头像：</label>
            <% if(product.picture != null && product.picture !== '') {%>
                 <img src="../<%=product.picture%>" width='100px' height='100px'/>
             <%}else{%>
                 <img src='../images/y.jpg'  width='80px' height='80px'/>
            <%}%>
            <input class="form-control" type="file" name="upHeaderImg" />
        </div>
        <div class="form-group"><input class="btn btn-primary" type='submit' value='确定 '/></div>
    </form>
</body>
</html>